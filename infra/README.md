# Project-level Terraform

The lowest-level configuration, split by lifecycle: each directory below is applied — or imported —
on its own schedule, and says what it is.

<!-- 🎨 TEMPLATE POST-EJECT: Apply this configuration -->
