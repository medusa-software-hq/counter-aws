# Identity plane

One instance per environment, in a workspace of the same name.

Applied with the operator's credentials rather than by CI: the sign-in path is not something a
deploy should be able to alter.
