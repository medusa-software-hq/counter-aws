# Shared infrastructure

What exists once for the account, whatever the environment. One state, no workspaces.

Applied with the operator's credentials: it manages IAM the CI/CD role deliberately cannot touch,
including the role itself.
